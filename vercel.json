{
    "installCommand": "python3 -m ensurepip; python3 -m pip install --break-system-packages -r requirements.txt",
    "buildCommand": "PATH=\"$PATH:/python312/bin\" bash -x ./build.sh PRIVATE",
    "outputDirectory": "public",
    "routes": [
    ],
    "github": {
        "silent": true
    }
}